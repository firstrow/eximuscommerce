TODO:
	- Create module update page for "SystemModules".
	- Add "translit button" on page update view.
	- Create initial "Pages migration" with table sctructure and indexes.
	- Add indexes to Page table
	- Add indexes to PageCategory table
	- Search and get done all "TODO" in source files.
	- Fix css3button to not hover on jquery buttons in dialog.
	- Create "hooks/events" for AR models using attachEventHandler
		Workfolw:
		1. Scan installed modules for events.php
		sample event record:
			array('Page', 'onAfterDelete', array($object, 'eventHandler'))
		2. Attach aviable class handlers in BaseModel::init
		sample:
			if (get_class($this))
				$this->attachEventHandler(args*)

	Done:
	+ Finish job with SGridView filet button.
	+ Get styled gridview pagination.
	+ Add to SGridView "Clear filter" link.
	+ Fix grid view loading css loading class.
	+ Add to SGridView "save filter" link.
	+ Fix SGridView th gradient style to respect not only -moz* browsers.
	+ Fix SGridView tr left and right rounded corners in firefox and opera.
	+ Remove gridview filter button in modules.
	+ SGridView display filter button only when "$filterMenu" is enabled.
	+ Fix buttons in opera browser.
	+ Translate ext.SGridView
	+ Translate ext.Sidebartabs
	+ Translate components.*
	+ Translate modules.admin
	+ Translate modules.core
	+ Translate modules.pages
	+ Translate modules.users
	+ Translate all existing modules.

